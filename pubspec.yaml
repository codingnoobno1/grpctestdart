name: grpc_main
description: A root entry point for the gRPC debug prototype.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  grpc_debug_server:
    path: grpc_debug_prototype/server
  grpc: ^5.1.0
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
