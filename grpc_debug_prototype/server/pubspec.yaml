name: grpc_debug_server
description: A prototype gRPC server for debugging.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  grpc: ^5.1.0
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
